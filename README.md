CalculadoraSwing/        ← Pasta do projeto
│
├── src/                 ← Código-fonte
│   ├── module-info.java ← (opcional, se usar módulos)
│   └── calculadora/     ← Pacote principal
│       └── Calculadora.java
│
└── build/               ← Gerado pelo NetBeans (classes compiladas)
